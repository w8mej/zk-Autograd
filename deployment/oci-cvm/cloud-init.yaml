#cloud-config
#
# Cloud-init configuration for OCI CVM instances.
# Installs Python dependencies and starts the prover service.

packages:
  - python3
  - python3-pip
runcmd:
  - cd /opt/zk-autograd && pip3 install -e .
  - nohup uvicorn prover.service:app --host 0.0.0.0 --port 8000 &
